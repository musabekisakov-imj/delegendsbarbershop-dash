# Base URL of the BarberPro NestJS backend.
# Public endpoints live under /public — no auth required.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Tenant slug (single-tenant for now). Used in some UI copy.
NEXT_PUBLIC_TENANT_NAME=Kirpykla Vilnius
