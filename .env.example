# Copy to .env.local for local development. .env.local is gitignored.

# Base URL of the NestJS backend (server/). When unset, lib/http.ts falls
# back to http://localhost:3001/api/v1, which matches `npm run start:dev`
# in the server/ directory.
VITE_API_URL=http://localhost:3001/api/v1
